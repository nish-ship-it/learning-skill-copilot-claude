# MLOps Run — 2026-03-11

## Overview
Full MLOps pipeline run using the `validate-data`, `train-model`, and `evaluate-model` skills on the expanded dataset.

**Pipeline:** validate → preprocess → train (MLflow) → evaluate  
**Data:** `examples/data/sample.csv` — 220 rows × 9 features  
**MLflow experiment:** `income-prediction`

---

## Stage 1: Data Validation (`validate-data` skill)

```
Rows: 220, Columns: 9

Null rates:
  age: 2.3%  (5 rows)
  hours_per_week: 3.2%  (6 rows)

Class distribution:
  <=50K: 62.7%
  >50K: 37.3%

Validation: PASSED ✓
```

**Interpretation:**
- Low null rates (< 5%) — median imputation is safe
- Class imbalance (37% minority) — handled with `class_weight='balanced'`
- All required columns present, target values valid

---

## Stage 2: Preprocessing

```
Loaded 220 rows × 9 cols
Missing before imputation: 12 cells
Train: 176 rows | Test: 44 rows
Features (8): age, income, education_years, hours_per_week,
              occupation, marital_status, gender, native_country
Class balance in train — >50K: 66 | <=50K: 110
```

**Steps applied:**
1. `SimpleImputer(strategy='median')` on numeric columns
2. `LabelEncoder` on all 4 categorical features
3. `LabelEncoder` on target (`<=50K`→0, `>50K`→1)
4. `StandardScaler` on numeric features
5. 80/20 stratified train/test split

---

## Stage 3: Training — Experiment 1 (baseline)

**Run name:** `baseline`  
**Run ID:** `378acf89a3a7455d95a4e80ad4e2178c`  
**Params:** `n_estimators=100, max_depth=5, class_weight='balanced'`

```
accuracy:  0.7727
precision: 0.7143
recall:    0.6250
f1:        0.6667
roc_auc:   0.7946
```

**Top features by importance:**
| Feature | Importance |
|---|---|
| income | 0.3083 |
| age | 0.1793 |
| occupation | 0.1471 |

---

## Stage 3: Training — Experiment 2 (deep)

**Run name:** `deep`  
**Run ID:** `d0560d6f2cc642afa0ee4c1a2f65f60d`  
**Params:** `n_estimators=200, max_depth=8, class_weight='balanced'`

```
accuracy:  0.7500
precision: 0.7273
recall:    0.5000
f1:        0.5926
roc_auc:   0.7857
```

---

## Stage 4: Evaluation Leaderboard (`evaluate-model` skill)

```
  run_id   run_name  accuracy     f1  roc_auc  precision  recall  n_estimators  max_depth
378acf89   baseline    0.7727  0.6667   0.7946     0.7143   0.625           100          5
d0560d6f       deep    0.7500  0.5926   0.7857     0.7273   0.500           200          8
```

**Winner: `baseline` (n_estimators=100, max_depth=5)**

---

## Key Findings

| Finding | Detail |
|---|---|
| `income` is top feature | 30.8% importance — most predictive of bracket |
| Deeper tree hurt recall | max_depth=8 recall dropped from 62.5% → 50% (overfitting) |
| Class imbalance handled | `balanced` weight prevented trivial majority-class classifier |
| Imputation transparent | 12 missing cells filled; no rows dropped |
| All runs reproducible | `random_state=42` set throughout |

## Reproducing These Runs

```bash
# Baseline
python examples/src/pipeline_runner.py examples/data/sample.csv \
  --n-estimators 100 --max-depth 5 --run-name baseline

# Deep
python examples/src/pipeline_runner.py examples/data/sample.csv \
  --n-estimators 200 --max-depth 8 --run-name deep

# View leaderboard
python examples/src/evaluate.py

# Launch MLflow UI
mlflow ui --port 5000
```

## Skills Used

| Skill | Trigger phrase used | Outcome |
|---|---|---|
| `validate-data` | "validate my data" | Passed — 2 warnings, 0 errors |
| `train-model` | "train the model" | Two runs logged to MLflow |
| `evaluate-model` | "compare experiment runs" | Leaderboard + best-run identification |
